name: Deploy Backend → Azure App Service

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Package source
        run: |
          zip -r deploy.zip . \
            --exclude ".git/*" \
            --exclude "venv/*" \
            --exclude ".packages/*" \
            --exclude "*.db" \
            --exclude "__pycache__/*" \
            --exclude "*.pyc" \
            --exclude ".env"

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy (with remote Oryx build)
        run: |
          az webapp deploy \
            --name bora-floripa-api \
            --resource-group rg-borafloripa \
            --src-path deploy.zip \
            --build-remote true \
            --restart true \
            --timeout 1800

      - name: Set startup command
        run: |
          az webapp config set \
            --name bora-floripa-api \
            --resource-group rg-borafloripa \
            --startup-file "bash /home/site/wwwroot/startup.sh"
